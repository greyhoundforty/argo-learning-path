# basic-app.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: basic-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/greyhoundforty/your-repo.git
    targetRevision: HEAD
    path: examples/basic-app
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true